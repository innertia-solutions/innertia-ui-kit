# nuxt-core Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking. NO TESTING — ejecutar directo.

**Goal:** Crear `packages/core` como el paquete `@innertia-solutions/nuxt-core` — infraestructura pura del frontend (Preline, Pinia, Pusher/Socketi, composables utilitarios) sin CSS ni componentes visuales.

**Architecture:** Nuxt Layer que extienden los apps. Todo auto-importado. Los composables son pure JS/TS sin acoplamiento a auth ni visual. El paquete se instala con `extends: ['@innertia-solutions/nuxt-core']`.

**Tech Stack:** Nuxt 4, Pinia, Preline 3, Pusher-js, date-fns/dayjs, pnpm workspaces, npm publish.

---

## File Map

```
packages/core/
├── package.json                    # @innertia-solutions/nuxt-core
├── nuxt.config.ts                  # registra todo como Nuxt Layer
├── plugins/
│   ├── preline.client.ts           # autoInit + MutationObserver
│   └── dark-state.client.ts        # sincroniza isDark con clase .dark del DOM
├── composables/
│   ├── useRealtime.js              # Pusher: connect, subscribe, unsubscribe
│   ├── useDownload.js              # XHR download sin auth (headers como param)
│   ├── useDate.js                  # formateo fechas UTC→Santiago
│   ├── useDevice.js                # deviceId persistente en localStorage
│   ├── useTimeAgo.js               # "hace X minutos" reactivo con dayjs
│   └── useRutFormatter.js          # formato y validación RUT chileno
└── public/
    └── init-theme.js               # script anti-flash dark mode (va en <head>)
```

---

### Task 1: Scaffold del paquete

**Files:**
- Create: `packages/core/package.json`
- Create: `packages/core/nuxt.config.ts`

- [ ] **Crear `packages/core/package.json`**

```json
{
  "name": "@innertia-solutions/nuxt-core",
  "version": "0.1.0",
  "description": "Innertia Solutions — Nuxt core layer: plugins, composables and base config",
  "keywords": ["nuxt", "vue", "preline", "pinia", "pusher", "composables"],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/innertia-solutions/innertia-nuxt"
  },
  "publishConfig": {
    "access": "public"
  },
  "main": "./nuxt.config.ts",
  "exports": {
    ".": "./nuxt.config.ts"
  },
  "peerDependencies": {
    "nuxt": ">=4.0.0",
    "vue": ">=3.5.0"
  },
  "dependencies": {
    "preline": "^3.2.3",
    "@pinia/nuxt": "^0.11.3",
    "pinia": "^3.0.4",
    "pinia-plugin-persistedstate": "^4.7.1",
    "pusher-js": "^8.5.0",
    "uuid": "^13.0.0",
    "date-fns": "^4.1.0",
    "date-fns-tz": "^3.2.0",
    "dayjs": "^1.11.0",
    "@nuxtjs/seo": "^3.4.0"
  },
  "devDependencies": {
    "nuxt": "^4.4.2",
    "vue": "^3.5.0"
  }
}
```

- [ ] **Crear `packages/core/nuxt.config.ts`**

```ts
export default defineNuxtConfig({
  modules: [
    '@pinia/nuxt',
    'pinia-plugin-persistedstate/nuxt',
  ],
  imports: {
    dirs: ['composables'],
  },
})
```

- [ ] **Instalar dependencias del workspace**

```bash
cd /Users/guillermofarias/Sites/inertia/innertia-ui-kit
pnpm install
```

- [ ] **Commit**

```bash
cd /Users/guillermofarias/Sites/inertia/innertia-ui-kit
git add packages/core/package.json packages/core/nuxt.config.ts
git commit --no-gpg-sign -m "feat(core): scaffold @innertia-solutions/nuxt-core package"
```

---

### Task 2: Plugin Preline

**Files:**
- Create: `packages/core/plugins/preline.client.ts`

- [ ] **Crear `packages/core/plugins/preline.client.ts`**

```ts
declare global {
  interface Window {
    HSStaticMethods?: { autoInit?: () => void }
    HSSelect?: new (el: HTMLElement) => { destroy?: () => void }
    HSThemeAppearance?: { init?: () => void }
  }
}

export default defineNuxtPlugin(async () => {
  if (!process.client) return

  try {
    await import('preline')

    const initPreline = () => {
      try { window.HSStaticMethods?.autoInit?.() } catch (_) {}
      try { window.HSThemeAppearance?.init?.() } catch (_) {}
    }

    const performMultipleInits = () => {
      initPreline()
      setTimeout(initPreline, 50)
      setTimeout(initPreline, 200)
      setTimeout(initPreline, 500)
    }

    if (document.readyState === 'loading') {
      document.addEventListener('DOMContentLoaded', performMultipleInits)
    } else {
      nextTick(performMultipleInits)
    }

    const nuxtApp = useNuxtApp()
    nuxtApp.hooks.hookOnce('app:mounted', () => performMultipleInits())
    nuxtApp.hooks.hook('page:finish', () => requestAnimationFrame(performMultipleInits))

    const observer = new MutationObserver((mutations) => {
      const hasPreline = mutations.some(({ addedNodes }) =>
        Array.from(addedNodes).some((node) => {
          if (node.nodeType !== Node.ELEMENT_NODE) return false
          const el = node as Element
          return (
            el.querySelector?.('[data-hs-overlay],[data-hs-dropdown],[data-hs-select]') ||
            el.hasAttribute?.('data-hs-overlay') ||
            el.hasAttribute?.('data-hs-dropdown') ||
            el.hasAttribute?.('data-hs-select') ||
            (typeof el.className === 'string' && el.className.includes('hs-'))
          )
        })
      )
      if (hasPreline) {
        setTimeout(initPreline, 10)
        setTimeout(initPreline, 100)
      }
    })

    if (document.readyState === 'loading') {
      document.addEventListener('DOMContentLoaded', () =>
        observer.observe(document.body, { childList: true, subtree: true })
      )
    } else {
      observer.observe(document.body, { childList: true, subtree: true })
    }

  } catch (e) {
    console.warn('[nuxt-core] Error al cargar Preline:', e)
  }
})
```

- [ ] **Commit**

```bash
git add packages/core/plugins/preline.client.ts
git commit --no-gpg-sign -m "feat(core): add preline autoInit plugin with MutationObserver"
```

---

### Task 3: Plugin dark-state

**Files:**
- Create: `packages/core/plugins/dark-state.client.ts`

- [ ] **Crear `packages/core/plugins/dark-state.client.ts`**

```ts
export default defineNuxtPlugin(() => {
  if (!process.client) return

  const isDark = useState<boolean>('isDark', () => false)

  const updateTheme = () => {
    if (typeof document !== 'undefined') {
      isDark.value = document.documentElement.classList.contains('dark')
    }
  }

  let observer: MutationObserver | null = null

  const initializeTheme = () => {
    updateTheme()
    if (observer) observer.disconnect()
    observer = new MutationObserver(() => updateTheme())
    observer.observe(document.documentElement, {
      attributes: true,
      attributeFilter: ['class'],
    })
  }

  if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', initializeTheme)
  } else {
    nextTick(initializeTheme)
  }
})
```

- [ ] **Commit**

```bash
git add packages/core/plugins/dark-state.client.ts
git commit --no-gpg-sign -m "feat(core): add dark-state plugin — syncs isDark state with DOM"
```

---

### Task 4: Script anti-flash

**Files:**
- Create: `packages/core/public/init-theme.js`

- [ ] **Crear `packages/core/public/init-theme.js`**

```js
(function () {
  try {
    var theme = localStorage.getItem('hs_theme') || 'light';
    if (
      theme === 'dark' ||
      (theme === 'auto' && window.matchMedia('(prefers-color-scheme: dark)').matches)
    ) {
      document.documentElement.classList.add('dark');
    } else {
      document.documentElement.classList.remove('dark');
    }
  } catch (e) {
    console.warn('[nuxt-core] Error initializing theme:', e);
  }
})();
```

- [ ] **Commit**

```bash
git add packages/core/public/init-theme.js
git commit --no-gpg-sign -m "feat(core): add init-theme.js anti-flash script for dark mode"
```

---

### Task 5: Composables utilitarios (migración directa)

**Files:**
- Create: `packages/core/composables/useDate.js`
- Create: `packages/core/composables/useDevice.js`
- Create: `packages/core/composables/useTimeAgo.js`
- Create: `packages/core/composables/useRutFormatter.js`

- [ ] **Crear `packages/core/composables/useDate.js`**

Copiar íntegro desde `packages/ui/composables/useDate.js` — sin cambios, es pure JS.

```bash
cp packages/ui/composables/useDate.js packages/core/composables/useDate.js
```

- [ ] **Crear `packages/core/composables/useDevice.js`**

```bash
cp packages/ui/composables/useDevice.js packages/core/composables/useDevice.js
```

- [ ] **Crear `packages/core/composables/useTimeAgo.js`**

```bash
cp packages/ui/composables/useTimeAgo.js packages/core/composables/useTimeAgo.js
```

- [ ] **Crear `packages/core/composables/useRutFormatter.js`**

```bash
cp packages/ui/composables/useRutFormatter.js packages/core/composables/useRutFormatter.js
```

- [ ] **Commit**

```bash
git add packages/core/composables/useDate.js packages/core/composables/useDevice.js packages/core/composables/useTimeAgo.js packages/core/composables/useRutFormatter.js
git commit --no-gpg-sign -m "feat(core): migrate useDate, useDevice, useTimeAgo, useRutFormatter composables"
```

---

### Task 6: useDownload (sin auth)

**Files:**
- Create: `packages/core/composables/useDownload.js`

El original importa `useAuthStore` — en core no existe auth. Se refactoriza para aceptar `headers` como parámetro.

- [ ] **Crear `packages/core/composables/useDownload.js`**

```js
/**
 * Descarga un archivo usando XHR con soporte de progreso.
 * Los headers de autenticación los provee el app consumidor.
 */
export function useDownload() {
  const config = useRuntimeConfig()
  const baseUrl = config.public.apiBaseUrl

  /**
   * @param {string} url - ruta relativa al baseUrl
   * @param {object} params - query params (GET) o body (POST)
   * @param {object} options - { onProgress, method, headers }
   * @returns {Promise<{ blob: Blob, headers: object }>}
   */
  function download(url, params = {}, options = {}) {
    const {
      onProgress = null,
      method = 'GET',
      headers = {},
    } = options

    return new Promise((resolve, reject) => {
      const xhr = new XMLHttpRequest()
      let query = ''
      if (method === 'GET' && Object.keys(params).length) {
        query = '?' + new URLSearchParams(params).toString()
      }
      xhr.open(method, `${baseUrl}/${url}${query}`)
      Object.entries(headers).forEach(([k, v]) => xhr.setRequestHeader(k, v))
      xhr.responseType = 'blob'
      xhr.onload = function () {
        const responseHeaders = {}
        xhr.getAllResponseHeaders().split('\r\n').forEach(line => {
          const [key, value] = line.split(': ')
          if (key) responseHeaders[key.toLowerCase()] = value
        })
        if (xhr.status >= 200 && xhr.status < 300) {
          resolve({ blob: xhr.response, headers: responseHeaders })
        } else {
          reject(new Error(`Download failed: ${xhr.status}`))
        }
      }
      xhr.onerror = () => reject(new Error('Network error'))
      xhr.onprogress = (event) => {
        if (onProgress && event.lengthComputable) {
          onProgress(Math.round((event.loaded / event.total) * 100), event)
        }
      }
      xhr.send(method === 'GET' ? null : JSON.stringify(params))
    })
  }

  return { download }
}
```

- [ ] **Commit**

```bash
git add packages/core/composables/useDownload.js
git commit --no-gpg-sign -m "feat(core): add useDownload — auth-agnostic XHR download with progress"
```

---

### Task 7: useRealtime (Pusher/Socketi)

**Files:**
- Create: `packages/core/composables/useRealtime.js`

Migrado de asetio. Lee credenciales de `runtimeConfig.public`. Soporta Socketi (wsHost configurable).

- [ ] **Crear `packages/core/composables/useRealtime.js`**

```js
import Pusher from 'pusher-js'
import { ref, readonly } from 'vue'

const pusher = ref(null)
const connected = ref(false)
const error = ref(null)
const subscribedChannels = ref({})
let alreadyConnected = false

export function useRealtime() {
  const config = useRuntimeConfig()
  const {
    pusherAppKey,
    pusherAppCluster,
    pusherWsHost,
    pusherWsPort,
  } = config.public

  const connect = () => {
    if (alreadyConnected || pusher.value) return

    if (!pusherAppKey) {
      error.value = '[nuxt-core] Falta runtimeConfig.public.pusherAppKey'
      console.error(error.value)
      return
    }

    const options = {
      cluster: pusherAppCluster || 'mt1',
      forceTLS: true,
      enabledTransports: ['ws', 'wss'],
    }

    // Socketi / host personalizado
    if (pusherWsHost) {
      options.wsHost = pusherWsHost
      options.wsPort = pusherWsPort ? Number(pusherWsPort) : 443
      options.wssPort = pusherWsPort ? Number(pusherWsPort) : 443
    }

    pusher.value = new Pusher(pusherAppKey, options)

    pusher.value.connection.bind('connected', () => {
      connected.value = true
      alreadyConnected = true
    })

    pusher.value.connection.bind('error', (err) => {
      error.value = err
      connected.value = false
      alreadyConnected = false
      console.error('[nuxt-core] Realtime error:', err)
    })

    pusher.value.connection.bind('disconnected', () => {
      connected.value = false
      alreadyConnected = false
    })
  }

  const subscribe = (channelName, eventHandlers = {}) => {
    if (!pusher.value) {
      console.warn(`[nuxt-core] Realtime no conectado. Canal "${channelName}" ignorado.`)
      return null
    }

    if (subscribedChannels.value[channelName]) {
      const channel = subscribedChannels.value[channelName]
      Object.entries(eventHandlers).forEach(([event, handler]) => {
        channel.unbind(event)
        channel.bind(event, handler)
      })
      return channel
    }

    const channel = pusher.value.subscribe(channelName)
    Object.entries(eventHandlers).forEach(([event, handler]) => {
      channel.bind(event, handler)
    })
    subscribedChannels.value[channelName] = channel
    return channel
  }

  const unsubscribe = (channelName) => {
    if (pusher.value && subscribedChannels.value[channelName]) {
      pusher.value.unsubscribe(channelName)
      delete subscribedChannels.value[channelName]
    }
  }

  const disconnect = () => {
    if (pusher.value) {
      Object.keys(subscribedChannels.value).forEach(ch => pusher.value.unsubscribe(ch))
      subscribedChannels.value = {}
      pusher.value.disconnect()
      pusher.value = null
      connected.value = false
      alreadyConnected = false
    }
  }

  return {
    pusherInstance: readonly(pusher),
    connected: readonly(connected),
    error: readonly(error),
    connect,
    disconnect,
    subscribe,
    unsubscribe,
  }
}
```

El app declara en `runtimeConfig`:
```ts
runtimeConfig: {
  public: {
    pusherAppKey: process.env.PUSHER_APP_KEY,
    pusherAppCluster: process.env.PUSHER_APP_CLUSTER,
    pusherWsHost: process.env.PUSHER_WS_HOST,   // opcional — Socketi
    pusherWsPort: process.env.PUSHER_WS_PORT,   // opcional
  }
}
```

- [ ] **Commit**

```bash
git add packages/core/composables/useRealtime.js
git commit --no-gpg-sign -m "feat(core): add useRealtime — Pusher/Socketi with configurable wsHost"
```

---

### Task 8: Registrar plugins en nuxt.config.ts

**Files:**
- Modify: `packages/core/nuxt.config.ts`

- [ ] **Actualizar `packages/core/nuxt.config.ts`**

```ts
export default defineNuxtConfig({
  modules: [
    '@pinia/nuxt',
    'pinia-plugin-persistedstate/nuxt',
    '@nuxtjs/seo',
  ],
  plugins: [
    '~/plugins/preline.client',
    '~/plugins/dark-state.client',
  ],
  imports: {
    dirs: ['composables'],
  },
})
```

- [ ] **Commit**

```bash
git add packages/core/nuxt.config.ts
git commit --no-gpg-sign -m "feat(core): register preline + dark-state plugins in nuxt.config"
```

---

### Task 9: Publicar en npm

- [ ] **Verificar que pnpm-workspace.yaml incluye el paquete**

`pnpm-workspace.yaml` ya tiene `packages/*` — no requiere cambio.

- [ ] **Instalar deps del workspace**

```bash
cd /Users/guillermofarias/Sites/inertia/innertia-ui-kit
pnpm install
```

- [ ] **Publicar**

```bash
cd packages/core
pnpm publish --no-git-checks --access public
```

Expected output:
```
npm notice Publishing to https://registry.npmjs.org/ with tag latest and public access
+ @innertia-solutions/nuxt-core@0.1.0
```

- [ ] **Commit final**

```bash
cd /Users/guillermofarias/Sites/inertia/innertia-ui-kit
git add packages/core/
git commit --no-gpg-sign -m "feat: publish @innertia-solutions/nuxt-core@0.1.0"
```

---

### Task 10: Conectar olimpo a nuxt-core

Verificar que olimpo puede usar el paquete publicado.

**Files:**
- Modify: `/Users/guillermofarias/Sites/inertia/olimpo/frontend/package.json`
- Modify: `/Users/guillermofarias/Sites/inertia/olimpo/frontend/nuxt.config.ts`

- [ ] **Agregar dependencia en olimpo**

```bash
cd /Users/guillermofarias/Sites/inertia/olimpo/frontend
pnpm add @innertia-solutions/nuxt-core
```

- [ ] **Actualizar `nuxt.config.ts` de olimpo**

Agregar `@innertia-solutions/nuxt-core` al `extends`:

```ts
export default defineNuxtConfig({
  extends: [
    '@innertia-solutions/nuxt-core',
    '@innertia-solutions/ui',         // se reemplazará por nuxt-themes en siguiente iteración
  ],
  // ...resto igual
})
```

- [ ] **Eliminar el plugin preline local de olimpo** (ahora lo provee nuxt-core)

```bash
rm /Users/guillermofarias/Sites/inertia/olimpo/frontend/app/plugins/preline.client.ts
```

- [ ] **Agregar init-theme.js al head en nuxt.config.ts de olimpo** (ya existe, verificar)

```ts
app: {
  head: {
    script: [
      { src: '/init-theme.js', tagPosition: 'head' }
    ]
  }
}
```

- [ ] **Levantar olimpo y verificar que no hay errores**

```bash
cd /Users/guillermofarias/Sites/inertia/olimpo/frontend
pnpm dev
```

Expected: servidor arranca sin errores, dark mode funciona, Preline inicializa.

- [ ] **Commit olimpo**

```bash
git add package.json nuxt.config.ts
git rm app/plugins/preline.client.ts
git commit --no-gpg-sign -m "chore: migrate to @innertia-solutions/nuxt-core"
```
